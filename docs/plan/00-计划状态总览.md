# HuoziWriter 方案状态总览

> 更新时间：2026-04-29  
> 目的：统一说明 `docs/plan` 下各方案的定位、当前状态与优先级关系，避免重复复核同一事项

## 1. 当前口径

- 全局冲突规则：若 `docs/plan` 下多份方案存在冲突，以时间更晚、且明确覆盖口径的方案为准；若新方案已显式声明优先级，则按新方案执行
- `01-07`：基础架构、页面、核心功能、数据模型、微信集成、套餐矩阵、知识层等底座方案
- `08`：二期总纲，当前已落地，但其中内容工作流细则已被 `09` 和 `10` 进一步细化
- `09`：内容工作流三次优化方案，当前已落地；若与 `08` 冲突，以 `09` 为准
- `10`：高价值后续优化与体验提效方案，当前已完成；用于补齐 `08/09` 之后的高价值优化项
- `11`：写作版 autoresearch 自动优化方案，当前已落地；作为后台自动优化系统，为 Prompt、风格注入和阶段产物提供自动评测闭环
- `12`：产品聚焦重构总方案，主链路与信息架构已基本落地；剩余视觉系统、组件抽象与移动端收口由 `16` 承接
- `13`：状态驱动写作与防单一风格优化方案，已落地为生成链路的写作状态核与多变体守护
- `14`：基于 hv-analysis 的深度研究与防空洞内容优化方案，已落地为研究简报、信源充分性与纵横交汇研究层
- `15`：UX 优化方案（产品价值导向），阶段二六大方向已 100% 落地，作为单点体感优化的执行记录
- `16`：UI/UX 全面重构方案 vNext，部分落地；主导航、核心页面骨架、工作区 step shell、命令面板、通知中心与全局 toast/banner 基础能力已形成，后台高频表格的移动端 card fallback 已继续扩到 `plan17 acceptance / business`，稿件详情 topbar 也已补入显式更多操作入口与删除稿件动作，稿件详情 research ribbon 下方也已补“当前稿件任务”分流卡，把研究缺口 / 正文空白 / 发布阻塞 / 结果待回流直接收口成显式入口，`/warroom` 已补首屏“待处理作战任务”分流，`/articles` 已补首屏“待处理稿件任务”分流，`/reviews` tabs 已补齐完整键盘可达性，且已补顶部“待处理复盘任务”分流，`/warroom` focus 区与设置总览“最近沉淀”也已补成显式跳转入口，待回流区也已补闭环提示与默认复盘引导，选题裂变面板也已补目的地约束与系列/backlog 落点提示，`/settings/author` 已补默认人设 / 系列 / backlog / 风格资产的显式待处理经营队列，`/settings/account` 已补密码 / 订阅 / 额度 / 存储 / 连接上限的显式待处理风险队列，`/settings/sources` 也已补齐待处理信源任务分流、停用来源恢复闭环与显式优先级编辑流，`/settings/language-guard` 已补规则编辑、重复拦截、近 30 天命中扫描与高频规则处理队列，`/settings/assets` 已补处理队列与库存分流入口，`/settings/publish` 已补默认连接缺失 / 异常连接 / 失败同步 / 待回流发布的显式处理队列，`/settings/intelligence-kb` 已补失效连接 / 无启用 KB / 待同步 KB / 待启用 KB 的显式收口；剩余设计系统统一、组件抽象、薄壳化与移动端细部均不纳入当前业务功能落地口径
- `17`：基于笔尖方法论的「三层分离 + 爆点结构化」优化方案，代码能力已基本收口；2026-04-27 最新运行态为 `functional=passed / quality=partial / business=partial / nonFunctional=passed`，总体仍为 `partial`。文风资产“无绑定自动选优、无资产自动补建”已落地，业务侧已不再被“无风格画像资产”硬阻塞；剩余阻塞集中在 `11.2 / 11.3` 的样本、人工标注与 30 天业务观察窗，且已补 blocker report / 人工标注 worklist-import 辅助工具。`writer` 端允许按 `22` 新增 `/automation` 自动驾驶入口，但不得破坏 作战台 / 稿件 / 复盘 / 设置 四个核心导航的主链路顺序
- `18`：IMA 知识库作为高价值信源的工作流集成方案，已落地；腾讯 IMA OpenAPI 已作为机会阶段爆点雷达与证据阶段检索导入的真实数据源接入
- `19`：公众号标题模式集成方案，已落地；基于 `docs/标题模式学习.md` 的「三要素 + 禁止清单 + 6 候选打分 + ⭐推荐」，已把 outlinePlanning.titleOptions 从 3 扩到 6，并补上评分、三要素命中、禁词校验、独立 `title_optimizer` prompt 与发布前标题体检闸门；不新增主链路阶段
- `20`：高价值优化与工程债收口方案，部分落地；业务链路、E2E 回归、migration 目录化、`schema-bootstrap.ts` 兼容层退化与 prompt cache article-linked 观测/报告脚本已继续收口，当前剩余主债收敛为 prompt cache 真验收 / `17` 的真实运行验收与数据观察窗；`17` 已补 blocker report 与人工标注辅助脚本
- `21`：公众号开头模式集成方案，已落地；开头候选、开头体检、`opening_optimizer` prompt、`/reviews` 独立入口、工作区显式入口、评测维度与端到端 smoke 均已接入，后续仅保留文档归档与业务观察窗
- `22`：全自动文章生产线与专有 Prompt 重构方案，M1-M5 `draftPreview` 已通过真实验收；验收脚本已收紧质量闸门，事实高风险、内容类发布阻塞、研究信源不足或 HTML 不同步都会判定失败。最新报告 `artifacts/plan22/real-automation-run-2026-04-26T124823-152Z.json` 显示 `runId=76`、`articleId=92`、`status=passed`；剩余未纳入本轮的是 `wechatDraft` 草稿箱真实同步和三场景同轮全通过。
- `24`：基于 IMA 与开源微信全文链路的百篇爆文分析方案，已完成链路校正、WeWe RSS 本地执行资产和 100 篇正文级结构化细读；IMA 只作为爆文候选与题材线索，正文级样本优先通过 `cooderl/wewe-rss` / `rachelos/we-mp-rss` 获取微信原文链接和全文。样本机制已沉淀为代码侧隐藏上游 `ViralGenomePack`，并进一步结构化为 `mechanismBias / firstScreenPromise / shareTrigger / authorPosture / evidencePriorities / emotionVectors / visualRhythmSlots`，接入质量 brief、深写作执行卡、文中配图规划与终稿 fallback；不增加用户手动步骤，仍保持全自动生成。
- `25`：用户侧功能能力优化与个性化写作提升方案，首轮能力已落地；聚焦“更稳定的人设、更像作者本人的表达、更高的信息密度、以及作者级结果回流”，不新增主链路阶段，当前已完成运行时稳定度、召回排序层、信息增益审计、作者级学习闭环，以及轻反馈到生成期表达约束的首轮闭环
- `26`：基于 WeWe 新百篇商业爆文样本的生成链路优化方案，已完成样本重采与结论沉淀；用于把商业写作从泛商业判断继续收窄到“对象 + 变化 + 钱流 + 决策代价 + 适用边界”
- `27`：基于 AIWriteX 高价值能力的选择性吸收优化方案，已完成调研与方案沉淀；明确不整体引入 AIWriteX、不复制源码或模板，仅原生重写中文热点源、热点评分、创意镜头、参考融合、模板导入和图片资产等高价值机制
- 按“业务功能是否缺失”的口径复核：`11-21` 当前已不存在新的主链路业务功能空白；`22` 是新的全链路自动化与 Prompt 专业化升级目标，不改变既有能力落地结论；剩余未到 `100%` 的主要是 `16/20` 的体验/工程债，以及 `17` 的数据验收阻塞，不应再混记为“业务未落地”

## 2. 状态总表

| 方案 | 主题 | 当前状态 | 备注 |
| --- | --- | --- | --- |
| `01` | 总体架构与部署 | 已形成基础实现 | 作为底座方案参考 |
| `02` | 页面路由与设计系统 | 已形成基础实现 | 作为 UI/路由基线参考 |
| `03` | 七大核心功能实施 | 已形成基础实现 | 核心功能已进入当前产品 |
| `04` | 数据模型与 API 契约 | 已形成基础实现 | 当前数据结构已明显吸收本方案 |
| `05` | 微信草稿箱真实集成 | 已形成基础实现 | 现已进一步增强到可靠发布与失败恢复 |
| `06` | 套餐权限矩阵与阶段 | 已形成基础实现 | 当前仅保留 `free / pro / ultra` |
| `07` | 结构化知识层增量 | 已形成基础实现 | 后续又被 `10` 纳入主链路强化 |
| `08` | 内容工作流二期总纲 | 已落地 | 总纲有效，内容工作流细则以后续 `09/10` 为准 |
| `09` | 内容工作流三次优化 | 已落地 | 内容工作流实现基线 |
| `10` | 高价值后续优化 | 已完成 | 已补齐 09 之后的体验与可靠性缺口 |
| `11` | 写作自动优化闭环 | 已落地 | 作为后台自动优化系统存在，前台归位规则见 `12`；验收与历史审计已并入 `11-写作版 autoresearch 自动优化方案.md` |
| `12` | 产品聚焦重构总方案 | 主链路已基本落地 | vNext 主导航、六步稿件链路、设置分区与回流主路径已形成 |
| `13` | 状态驱动写作与防单一风格 | 已落地 | 写作状态核、DNA v2、原型库与多变体守护已注入生成链路 |
| `14` | hv-analysis 深度研究与防空洞 | 已落地 | 研究简报 + 五维信源充分性 + 纵横交汇分析作为发布前硬约束 |
| `15` | UX 优化方案（产品价值导向）| 已落地（阶段二完成）| 沉浸/白板/语感/手稿/微信预览/空状态六个方向已 100% 落地 |
| `16` | UI/UX 全面重构方案 vNext | 部分落地 | 信息架构、核心页面骨架、命令面板、通知中心与全局 toast/banner 基础能力已落地；后台高频表格移动端 fallback 已继续补到 `plan17 acceptance / business`，稿件详情 topbar 已补显式更多操作菜单与删除稿件动作，稿件详情 research ribbon 下方也已补“当前稿件任务”分流卡，`/warroom` 已补待处理作战任务分流，`/articles` 已补待处理稿件任务分流，`/reviews` tabs 已补齐完整键盘可达性并新增待处理复盘任务分流，`/warroom` focus 区与设置总览“最近沉淀”已补显式跳转入口，待回流区已补闭环提示与默认复盘引导，选题裂变面板已补目的地约束与系列/backlog 落点提示，`/settings/author` 已补待处理作者经营任务，`/settings/account` 已补待处理账号与配额风险，`/settings/sources` 已补待处理信源任务分流、停用来源恢复闭环与显式优先级编辑流，`/settings/language-guard` 已补规则编辑、重复拦截、近 30 天命中扫描与高频规则处理队列，`/settings/assets` 已补处理队列与库存分流入口，`/settings/publish` 已补待处理发布任务分流，`/settings/intelligence-kb` 已补待处理智库入口收口；剩余设计系统统一、组件层抽象、薄壳化与移动端细部从当前业务功能收口范围移出 |
| `17` | 三层分离 + 爆点结构化 | 代码能力已落地，自动验收仍 partial | 2026-04-27 真实运行态为 `functional=passed / quality=partial / business=partial / nonFunctional=passed`；文风资产自动选优与自动补建已生效，当前剩余阻塞是 `topicFissionEval / strategySpearman / evidenceHookPR / rhythmCorrelation / authorLiftVsBaseline / fissionVsRadar / matrixWeeklyOutput / styleHeatmapUsage` 这些样本与观察窗项，不应误判为 100% |
| `18` | IMA 知识库高价值信源集成 | 已落地 | IMA 凭证、知识库绑定、机会阶段雷达与证据检索导入均已形成闭环 |
| `19` | 公众号标题模式集成 | 已落地 | 已把 `docs/标题模式学习.md` 的「三要素 + 禁止清单 + 6 候选打分 + ⭐推荐」注入 outlinePlanning.titleOptions（3→6）+ 新建 `title_optimizer` 独立 prompt + 发布守卫加标题体检；不新增主链路阶段 |
| `20` | 高价值优化与工程债收口 | 部分落地 | 网关、回流、migration 目录化、E2E 服务链、`schema-bootstrap.ts` 兼容层退化、prompt cache article-linked 观测与核心业务 smoke 已收口；剩余主债是 prompt cache 真验收 / `17` 数据验收观察窗，已补 blocker report 与人工标注辅助脚本 |
| `21` | 公众号开头模式集成 | 已落地 | `opening-patterns.ts`、`openingOptions(3)`、发布前开头体检、`opening_optimizer` prompt/scene、`/reviews` 独立入口、工作区显式入口、writing-eval 维度与 smoke 验收均已接入 |
| `22` | 全自动文章生产线与专有 Prompt 重构 | `draftPreview` 已真实验收 | 自动编排、自动驾驶舱、SSE、阶段重跑、SearXNG 搜索、专有 Prompt、图片探针和 `draftPreview` 终稿链路已落地；已补 AI 请求全生命周期超时、OpenAI 健康探针兼容、事实风险前置拦截、语言守卫噪声刷新、draftPreview/WechatDraft 发布守门分级；当前文本 AI 统一走 OpenAI 路由，最新真实报告 `status=passed` |
| `24` | IMA + 微信全文链路百篇爆文分析 | 已吸收到生成链路 | IMA 不再作为正文直取入口，优先用 WeWe RSS / We-MP-RSS 获取原文链接和全文；脚本已补 `--source wewe-db` 单篇抓取、配图时机/来源分析、招聘公告过滤、题材配额、账号上限和多样性硬验收。2026-04-28 已完成 100 篇正文级分析：7 类题材、13 个账号，最大题材占比 16%，最大账号占比 8%；样本基因已进一步结构化为 `mechanismBias / firstScreenPromise / shareTrigger / authorPosture / evidencePriorities / emotionVectors / visualRhythmSlots`，并接入质量 brief、深写作执行卡、配图规划与终稿 fallback |
| `25` | 用户侧功能能力优化与个性化写作提升 | 首轮能力已落地 | 已完成运行时稳定度、个性化表达底座、信息密度审计、作者级学习闭环、轻反馈驱动的生成期表达修正、题材 / 情绪 / 受众动态表达召回，以及跨文章正反例表达样本沉淀首轮实现 |
| `26` | WeWe 新百篇商业爆文样本优化 | 已完成样本重采与结论沉淀 | 用新百篇商业样本校正生成链路，强调对象、变化、钱流、决策代价、证据压判断与商业写作姿态 |
| `27` | AIWriteX 高价值能力选择性吸收 | 方案已沉淀 | 不整体引入 AIWriteX，不复制源码或模板；优先原生重写中文热点源 Adapter、热点评分、创意维度镜头和参考文章融合比例 |

## 3. 阅读顺序

如果要理解当前产品，建议按以下顺序阅读：

1. `24-基于IMA百篇爆文样本的全自动生成流程根源优化方案.md` — 最新百篇爆文分析链路，当前已从 IMA 标题反查改为 WeWe RSS / We-MP-RSS 正文入口优先；本地已完成 100 篇正文级细读和配图时机/来源分析，并已通过 `ViralGenomePack` 吸收到自动生成链路
2. `27-基于AIWriteX高价值能力的选择性吸收优化方案.md` — 中文热点源、热点评分、创意镜头、模板导入和参考融合的选择性吸收方案
3. `26-基于WeWe新百篇商业爆文样本的生成链路优化方案.md` — 商业写作从泛商业继续收窄到钱流、决策与适用边界
4. `22-全自动文章生产线与专有Prompt重构方案.md` — 当前自动化、搜索、Prompt 与真实验收总方案
5. `12-产品聚焦重构总方案.md` — vNext 主链路与边界
6. `16-UI_UX全面重构方案_vNext.md` — vNext 形态层（设计系统、信息架构、页面线框、落地路线图）
7. `13-状态驱动写作与防单一风格优化方案.md` — 生成层的写作状态核
8. `14-基于 hv-analysis 的深度研究与防空洞内容优化方案.md` — 研究层
9. `15-UX_OPTIMIZATION_PLAN.md` — 已落地的单点体感优化记录
10. `08-内容工作流二期完整优化方案.md` / `09` / `10` / `11` — 内容工作流与后台自动优化的实现底座
11. 再回看 `05 / 06 / 07` 了解发布、套餐、知识层的底层约束

## 4. 优先级规则

- 若 `16` 与 `02 / 15` 在视觉系统、组件库、信息架构粒度上冲突，以 `16` 为准（`02` 与 `15` 中已落地的具体实现保留为参考）
- 若 `12` 与 `08/09/10/11` 冲突，以 `12` 为准
- 若 `08` 与 `09` 冲突，以 `09` 为准
- 若 `09` 与 `10` 冲突，以已经落地的 `10` 实现为准
- 若 `11` 与 `09/10` 冲突，以已落地的 `09/10` 为准；`11` 在 vNext 中被归位为后台自动优化系统，前后台边界以 `12` 为准
- 若 `22` 与 `12 / 14 / 16 / 18 / 19 / 20 / 21` 在自动化编排、搜索底座、专有 Prompt、真实数据验收或主界面默认交互上冲突，以 `22` 作为下一阶段实施目标；既有方案中的已落地能力仍作为可复用底座
- 若 `24` 与 `13 / 17 / 18 / 19 / 21 / 22` 在爆文样本如何进入生成链路上冲突，以 `24` 为新的上游生成逻辑；`24` 不替代既有标题、开头、研究和发布守卫，只给它们提供同赛道爆文基因输入
- 若 `27` 与既有生成、选题、模板、发布或图片方案冲突，优先保留既有 TypeScript/Next.js 底座与已落地流水线；`27` 只作为选择性机制吸收方案，不作为引入 AIWriteX 运行时或复制源码/模板的授权依据
- 若基础方案与后续迭代方案冲突，以更晚且已落地的方案为准

## 5. 验证记录

- 2026-04-29：新增 `27 基于 AIWriteX 高价值能力的选择性吸收优化方案`（v0.1）；调研 AIWriteX 后确认不整体引入 Python/CrewAI/AIForge 运行时，不复制源码和模板，仅吸收中文热点源、热点评分、创意维度镜头、参考文章融合比例、微信模板导入校验、发布边界增强与图片资产工作流等机制，并要求全部在当前 TypeScript/Next.js 架构内原生重写。
- 2026-04-25：新增 `22 全自动文章生产线与专有 Prompt 重构方案`；明确产品从“人工驱动阶段工作台”升级为“公众号增长写作 AI 生产代理”，采用自托管 SearXNG 作为无需 key 的搜索主方案，DDGS 仅作开发/应急 fallback，并要求每阶段专有 Prompt 与 `.env` 真实数据验收
- 2026-04-28：更新 `24 基于 IMA 与开源微信全文链路的百篇爆文分析方案`；确认 IMA 官方搜索接口当前不能稳定批量返回微信原文 URL 和正文，正文级样本链路改为 `cooderl/wewe-rss` 优先、`rachelos/we-mp-rss` 备选、`wnma3mz/wechat_articles_spider` 补阅读/点赞指标、IMA 标题 + 搜狗反查仅兜底。新增 `scripts/plan24-fulltext-viral-analysis.py --source wewe` 与 `--check-wewe`，用于从 WeWe RSS JSON Feed 获取链接和全文后执行过滤与分析；补齐 `24-WeWe-RSS公众号种子池.json` 和 `24-WeWe-RSS本地全文采集运行手册.md`。后续继续完成 WeWe 授权、导入公众号订阅池，并补入配图时机、首图位置、图文密度、图片来源类型和域名分布分析；招聘/校招/实习类公告流已从爆文样本中过滤。
- 2026-04-28：继续收口 `24` 的生成链路吸收：新增 `article-viral-genome.ts`，把 100 篇样本的处境变化、开头机制、反说教契约、叙事机制和配图节奏作为隐藏上游输入；后续又将其进一步结构化为 `mechanismBias / firstScreenPromise / shareTrigger / authorPosture / evidencePriorities / emotionVectors / visualRhythmSlots`，接入 `article-prompt-quality-brief.ts`、`article-stage-artifacts.ts`、`article-visual-planner.ts` 与 `generation.ts`，补齐“开头强兑付 + fallback 抗教程化”；并通过 `pnpm exec tsx --test apps/web/src/lib/__tests__/article-viral-genome.test.ts apps/web/src/lib/__tests__/article-stage-artifacts-apply-command.test.ts apps/web/src/lib/__tests__/article-prompt-quality-brief.test.ts apps/web/src/lib/__tests__/generation-fallback.test.ts` 与 `pnpm --filter @huoziwriter/web type-check`。
- 2026-04-28：继续实施 `25` 的作者级学习闭环：已新增 `author_outcome_feedback_ledgers` 持久化表与 `author-outcome-feedback-ledger.ts` 聚合模块，把文章原型 / 文风状态 / 开头模式 / 段落节奏的 outcome 结果沉淀为作者级反馈账本，并接入 `getArticleWritingContext`、正文生成、开头预览、命令改写及 outcome 回写接口；已通过 `pnpm exec tsx --test apps/web/src/lib/__tests__/author-outcome-feedback-ledger.test.ts apps/web/src/lib/__tests__/generation-author-outcome-feedback-guide.test.ts apps/web/src/lib/__tests__/article-writing-context.test.ts apps/web/src/lib/__tests__/generation-fallback.test.ts` 与 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`。
- 2026-04-28：继续推进 `25` 的结果回流写回作者资产：已把作者级账本推荐回写到运行时 authoring style context，自动前置高命中 `openingPatterns / statePresets`，补充 persona 侧近期高命中原型 / 状态提示，并让 deepWriting 与正文生成在无显式指定时优先采用作者级原型 / 状态推荐；已通过 `pnpm exec tsx --test apps/web/src/lib/__tests__/article-authoring-style-context.test.ts apps/web/src/lib/__tests__/author-outcome-feedback-ledger.test.ts apps/web/src/lib/__tests__/generation-fallback.test.ts` 与 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`。
- 2026-04-28：继续推进 `25` 的“个人有效写法”显式化：已在作者级账本内新增 `effectiveWritingProfile` 摘要，把高命中开头、判断强度、段落节奏、文章原型整理成用户可读推荐，并接入 `/reviews` 页独立区块，避免结果学习只停留在隐式 prompt 层；已通过 `pnpm exec tsx --test apps/web/src/lib/__tests__/author-outcome-feedback-ledger.test.ts apps/web/src/lib/__tests__/generation-author-outcome-feedback-guide.test.ts` 与 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`。
- 2026-04-28：继续推进 `25` 的工作区前移：已把作者级 `effectiveWritingProfile` 从 `/reviews` 进一步前移到稿件工作区右侧 rail，作者写当前稿件时即可直接参考高命中开头 / 判断强度 / 节奏 / 原型建议；已通过 `pnpm exec tsx --test apps/web/src/lib/__tests__/personal-effective-writing-rail.test.tsx`。全量 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false` 仍被仓库既有的 `article-research-supplement.ts` 与 `article-viral-genome.ts` 类型错误阻塞，非本次改动引入。
- 2026-04-28：继续推进 `25` 的轻反馈回写：已在结果阶段新增“更像我 / 不像我 / 太硬 / 太软 / 太像教程 / 太像评论”表达反馈，持久化到 `article_outcomes.expression_feedback_json`，并纳入作者级结果账本打分，让显式个人反馈直接影响后续原型 / 状态 / 开头 / 节奏推荐；已通过 `pnpm exec tsx --test apps/web/src/lib/__tests__/author-outcome-feedback-ledger.test.ts apps/web/src/lib/__tests__/personal-effective-writing-rail.test.tsx` 与 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`。
- 2026-04-26：继续收口 `22` 的严格验收链路；已补 AI 请求全生命周期超时、OpenAI 健康探针兼容、事实风险默认审计不改稿、事实风险前置拦截、发布守门语言噪声刷新、篇幅归一化 AI 噪声扫描、fallback 开头/事实核查修正、`draftPreview` 缺封面降级为告警；最新真实验收报告为 `artifacts/plan22/real-automation-run-2026-04-26T124823-152Z.json`，结果 `status=passed`
- 2026-04-24：继续实施 `16` 的稿件详情顶栏收口：`WorkspaceShell` 已补显式“更多操作”菜单，可直接复制 Markdown、导出 Markdown/HTML/PDF、删除稿件，并跳转到稿件元信息区；同时新增 `DELETE /api/articles/[id]` 与仓储层清理；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的 warroom / settings 入口收口：`/warroom` 顶部 focus 区已补齐“今天写什么 / 在推什么 / 等什么结果 / 最近学到什么”四张显式跳转卡，并把选题总数改回真实总量；设置总览“最近沉淀”已改为可直接回跳到稿件详情或对应设置分区；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的信源生命周期收口：`/settings/sources` 已单列已停用自定义信源，并支持直接恢复；启用名额统计同步改为只计入 active 自定义源；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的信源排序收口：`/settings/sources` 中的 active 自定义信源已改成卡片内显式优先级输入与保存按钮，并显示当前排位，不再依赖 `window.prompt` 调整排序；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的作者经营入口收口：`/settings/author` 已把默认人设缺失、长期系列缺失、无活跃系列、系列未绑定人设、空选题库与风格资产缺口收口成“待处理作者经营任务”，并提供直达人设 / 系列 / backlog / 风格资产的页内入口；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的账号风险入口收口：`/settings/account` 已把密码更新、订阅停用、生成额度逼近上限、封面图额度逼近上限、图片空间逼近上限与公众号连接逼近上限收口成“待处理账号与配额风险”，并提供直达改密 / 账单 / 资产 / 发布连接的入口；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的 reviews 入口收口：`/reviews` 已把待补 plan17 质量标注、待补结果快照、待补结果标签与“差一点命中”收口成顶部“待处理复盘任务”分流区，可直接跳到质量补桶、快速打标签和差一点命中分区；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的 articles 首屏收口：`/articles` 已把草稿待推进、久未推进草稿、发布失败稿件与待回流结果收口成首屏“待处理稿件任务”分流区，可直接跳到草稿、发布阶段与结果补录入口；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的 warroom 首屏收口：`/warroom` 已把发布失败、超期待回流、在推稿件与今日优先题收口成首屏“待处理作战任务”分流区，并把发布失败提升到回流与开题之前；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的语言守卫收口：`/settings/language-guard` 中的现有规则已支持编辑、保存与重复拦截，不再只能删除后重建；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的资产中心收口：`/settings/assets` 已补出冲突背景卡 / 待刷新背景卡 / 待处理图片三类处理队列，并新增页内库存分流入口；资产中心主体也从“最近 6 条”改成“全量库存 + 最近展示”的结构；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的发布与智库入口收口：`/settings/publish` 已把默认连接缺失、异常连接、失败同步与待回流发布单独分流为“待处理发布任务”，并提供直达连接维护 / 稿件发布阶段入口；`/settings/intelligence-kb` 已补出失效连接、无可用连接、无启用 KB、待同步 KB 与待启用 KB 的显式处理队列，并保留回跳到 `warroom / articles` 的业务落点；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的待回流收口：`warroom` 待回流卡片与快速录入抽屉已补上“最后一窗 / 仍待判定时默认展开复盘字段、提示保存后是否仍留在待回流”的闭环引导；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的选题裂变收口：`warroom` 选题裂变面板已补出建议系列文案、当前起稿/入库落点提示，并按所选系列优先过滤 backlog，避免把候选放错系列或选题库；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的后台移动端收口：`/admin/plan17/acceptance` 与 `/admin/plan17/business` 的表格已补齐 mobile card fallback，手机端不再要求横向滚动查看 drilldown；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的壳层导航收口：marketing shell 已补移动端导航抽屉，writer topbar 已补账户 / 套餐态 chip，并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的全局反馈收口：新增 layout 级 toast viewport，支持优先级与最多 3 条并发显示，稿件/设置相关持久提醒改为共享 banner 组件，命令面板主题/专注切换补上即时 toast 反馈；并通过 `pnpm exec tsx --test apps/web/src/lib/__tests__/app-feedback-toast.test.ts` 与 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的 shell 级体验收口：writer/admin 通知中心已接入真实系统事件（微信推送成功/失败、token 过期/即将过期、后台失败任务、writing-eval 调度异常），`/admin/ai-routing/observations` 已补齐移动端 card fallback，并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-24：继续实施 `16` 的业务相关 UI 收口：命令面板已补齐 recent 分组、本地最近访问记忆、`Cmd/Ctrl + K`、`Escape`、上下键选择与 `Enter` 执行；`16` 状态从“暂停实施”校正为“部分落地”
- 2026-04-15：复核 `08 / 09 / 10` 状态与当前实现是否一致
- 2026-04-15：`08` 已同步为落地态并补充完成映射
- 2026-04-15：`10` 已补充完成映射与验收记录
- 2026-04-15：新增 `11`，用于规划写作版 autoresearch 自动评测与持续优化闭环
- 2026-04-15：新增 `12`，用于作为 vNext 产品聚焦重构的最新总纲
- 2026-04-15：补充 `11 -> 12` 映射规则，明确 `11` 保留为后台自动优化系统
- 2026-04-18：`11` 已同步为落地态，并补齐验收记录与历史 gap audit 口径说明
- 2026-04-18：补登 `13 / 14 / 15` 为已落地，新增 `16 UI/UX 全面重构方案 vNext` 作为形态层的最新口径
- 2026-04-19：新增 `17 基于笔尖方法论的三层分离 + 爆点结构化优化方案`，作为主链路内部的硬字段 / 硬闸门 / 硬 Prompt 注入增量；建议合并到 `16 M2` 交付
- 2026-04-19：`17` 对照 main 分支代码做 gap audit 后修订到 v0.2；关键变化是将笔尖四元从"新增 4 个独立字段"改为"映射层方案"（复用 13 号已落地的 Human Only Signals 作为底层数据，四元作为派生视图由 AI 即时聚合与自检）
- 2026-04-20：新增 `18 IMA 知识库高价值信源集成方案`（v0.1）；MVP 范围限定为凭证 + KB 配置 + 机会阶段爆点雷达 + 证据阶段 IMA 检索导入；明确不动 worker-py 周期任务、不在 generation 阶段隐式注入；作为 `17 §4.1 选题裂变` 的真实数据源补丁
- 2026-04-20：`18` 对照 main 分支代码做 gap audit 后修订到 v0.2；关键变化是把 IMA 集成定位从"新增 `ImaHookRadarPanel` 面板"改为"为已落地的 `WarroomTopicFissionPanel` + `topic-fission.ts` 注入真实 corpus"，零 UI 重建；取消新建的 hook-radar / convert-to-lead / evidence-import 三个端点，改为扩展既有 `/fission` 端点传 `engine` 参数；实施量从 13 新建 + 3 修改 减为 9 新建 + 4 修改
- 2026-04-20：新增 `19 公众号标题模式集成方案`（v0.1）；从 `docs/标题模式学习.md` 提炼「三要素 + 禁止清单 + 6 候选打分 + ⭐推荐」；最小入侵式扩展已落地的 `outlinePlanning.titleOptions`（3→6 + openRateScore + elementsHit + forbiddenHits + isRecommended）；新建 `title_optimizer` 独立 prompt 进 autoresearch 闭环；发布守卫加标题体检闸门
- 2026-04-20：`19` 已同步为落地态；标题相关 `pnpm type-check` 与 2 条 smoke 已通过，当前口径为“结构大纲与标题优化分离 + 发布前标题体检”
- 2026-04-21：新增 `20 高价值优化与工程债收口方案`（v0.1）；核验源码后把重点从「新增功能」转到「跨切面工程债」：LLM 网关零 Prompt 缓存（`lib/ai-gateway.ts:134-170`）、稿件工作区 11,846 行单文件（`components/article-workspace-client.tsx`）、schema-bootstrap 2,375 行硬编码、plan 17 验收阻塞已从代码转到数据侧；M0/M1/M2/M3 四个里程碑
- 2026-04-21：新增 `21 公众号开头模式集成方案`（v0.1）；从 `docs/开头模式学习.md` 提炼「四种死法（D1 大而空 / D2 自我介绍 / D3 引用诱饵 / D4 钩子埋藏） + 钩子三件套（具体入口 / 信息前置 / 第一屏兑付） + 200 字门槛 + 4 维诊断」；与 `19` 标题侧完全同构，构成「标题 + 开头」双闸门；复用 `13` 的 `writing-diversity` 6 类分类，仅追加质量上限标注；不新增阶段、不动信息架构
- 2026-04-22：复核 `17 / 18 / 20 / 21` 与当前代码真相；将 `17` 从“待实施”修正为“代码能力已落地但自动验收仍 partial”，将 `18` 修正为“已落地”
- 2026-04-22：补登 `20` 的当前真实状态为“部分落地”，已确认 M0 网关三件套、AI 观测、provider 健康页、待回流区块、复盘打标签与 migration runner 已有实现
- 2026-04-22：补登 `21` 的当前真实状态为“主要能力已落地”，已确认开头候选、开头体检、`opening_optimizer` prompt/scene 与 writing-eval 专项评测维度均已接入
- 2026-04-22：继续收口 `20`；`article-workspace-client.tsx` 当前约 `1,494` 行、`45KB`，新增 `article-workspace-derived-state.ts`、`article-workspace-shell-derived-state.ts`、`article-workspace-sync-effects.ts`、`article-workspace-local-effects.ts`、`article-workspace-local-handlers.ts`、`article-workspace-action-bundle.ts`、`article-workspace-action-bundle-input.ts`、`article-workspace-shell-bundle.ts`、`article-workspace-client-types.ts`、`article-workspace-client-state.ts` 与 `article-workspace-client-computed.ts`，继续外提大段派生状态、shell checklist、authoring phase、同步 effect、路由/自动保存 effect、本地 handler、整段 shell 装配逻辑、动作 bundle 入参装配、顶部类型/props 定义、成片客户端状态装配与本地计算 memo，并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`
- 2026-04-22：继续收口 `20` 的非薄壳项；补齐 `apps/web/src/app/api/articles/[id]/stages/[stageCode]/apply/stream/route.ts` 与前端消费，`deepWriting / factCheck / prosePolish` 的正文回写现在也支持流式返回，减少长改写时的黑盒等待
- 2026-04-22：继续收口 `20` 的非薄壳项；新增 `apps/web/src/lib/db-migrations.ts`，将 migration runner 下沉为脚本/runtime 共用实现，版本记录统一到 `schema_migrations`，并让 `ensureExtendedProductSchema()` 先跑 pending migrations 再做兼容补丁
- 2026-04-22：继续收口 `20` 的非薄壳项；`ensureExtendedProductSchema()` 与 `ensureRuntimeMigrations()` 已改为按数据库实例缓存，避免同进程下每次读写都重复跑整段 schema 兼容逻辑，并补上跨临时库切换的回归测试
- 2026-04-23：继续复核 `12 / 16 / 20 / 21` 的代码真相；确认 vNext 主导航、稿件六步链路、设置分区与旧入口迁移已基本落地，将 `12` 调整为“主链路已基本落地”，将 `16` 调整为“部分落地”
- 2026-04-23：继续收口 `16 / 20` 的移动端与薄壳债：已修正 writer 手机端仍先渲染二级说明栏的问题，工作区手机端不再默认堆叠完整侧栏与 inspector 双份辅助信息；`article-workspace-client.tsx` 同步清理拆分后的残留依赖，但 `16 / 20` 仍保持“部分落地”
- 2026-04-23：`20` 再次收口：Playwright E2E 服务链已切回稳定的 `next build + next start`，IMA 裂变 JSON/SSE 双形态契约与 publish guard 人类信号口径已修正，`pnpm test:e2e:smoke` 结果为 `56 passed / 1 skipped`
- 2026-04-23：`21` 已补齐端到端验收，开头候选、开头体检、`opening_optimizer` prompt/scene 与 smoke 均通过，状态由“主要能力已落地”提升为“已落地”
- 2026-04-23：继续收口 `21` 的作者侧独立入口：`/reviews` 中的「开头体检」按钮已补齐完整结果卡片，独立返回 4 维诊断、2 条改写方向、⭐ 推荐理由、命中死法与逐项检查结果，并补上 `opening-check-review` 纯逻辑回归测试
- 2026-04-23：继续收口 `20` 的网关回归：`ai-gateway.test.ts` 已补齐“连续 5 次 429 后切 fallback model”的真实 `generateSceneText()` 回归，连同既有 `retry-after` 与 `401 fatal` 测试一起覆盖 `4.2` 三项验收
- 2026-04-23：继续收口 `20` 的 Anthropic 兼容链路：`ai-gateway.test.ts` 已补齐“未传 `systemSegments` 时继续走旧 `fetch /v1/messages` 路径”的真实 `generateSceneText()` 回归，`4.1` 的兼容路径测试项已回写
- 2026-04-23：继续收口 `21` 的开头禁区：`opening-patterns.ts` 已补入“大家好，我是 X，今天聊聊”类自我介绍开场拦截，并补齐 grand-background / 强场景开头 / 自我介绍开头三条回归，回写 `4.3 / 4.4` 的本地验收口径
- 2026-04-23：继续回写 `21` 的本地验收状态：已通过 `tsc` 与 opening/title/opening-check/system-segments 相关 21 条单测，且确认 `openingOptimizer` 已出现在 `admin/ai-routing` 路由配置与 `admin/writing-eval` 专项卡片中，补齐多处历史未勾选项
- 2026-04-23：继续收口 `21` 的多样性对接：`writing-diversity.ts` 已把开头模式的质量上限写进重复提示文案，并新增 `writing-diversity.test.ts` 回归覆盖撞车提示与 balanced 两种情况
- 2026-04-23：继续收口 `21` 的候选选择：新增 `stage-selection-drafts.test.ts`，锁住大纲开头默认落到推荐项、且作者手动改选后不会再被推荐项覆盖
- 2026-04-23：继续收口 `21` 的候选风险呈现：新增 `outline-opening-option-tone.ts` 与回归测试，命中 `forbiddenHits` 的开头候选卡现在直接走红色 danger 样式，补齐大纲候选 UI 风险高亮口径
- 2026-04-23：继续收口 `21` 的接口级验收：定向执行 `playwright` smoke 用例 `publish guard blocks forbidden openings and warns on weak opening strength`，已确认「在当今 AI 时代」类开头会被发布守卫阻断，文档同步回写接口级验收项
- 2026-04-23：继续收口 `21` 的全链路验收：新增 `playwright` smoke 用例 `forbidden opening is caught from outline selection through opening check and publish guard rewrite`，已确认「在当今 AI 时代」类开头会从大纲选择进入开头体检、产出改写方向、在发布前被阻断，并在改写后解除阻断；同时补齐 plan 13 / plan 19 定向回归通过记录
- 2026-04-24：按“专注业务功能、薄壳化与移动端细部放弃实施”的最新口径回写 `16`：该方案转为暂停实施，不再作为当前业务功能 100% 落地阻塞；同时同步 `17` 文档中已自动通过的 `11.1` 功能验收与 `11.4` 非功能验收勾选状态
- 2026-04-24：继续收口 `21` 的 writing-eval 闭环：`plan21-opening-optimizer-v1` 默认入桶 30 条开头专项评测样本，并创建 `lead_only` 的 opening_optimizer 定时评测 schedule，保证新库可直接触发开头专项评测闭环
- 2026-04-24：继续收口 `20` 的 shadow traffic 业务验收：新增网关回归确认 `shadow_traffic_percent=10` 时命中用户会并行调用 shadow model、主返回仍使用 primary；新增 writing-eval primary/shadow run 对比 helper，可按 paired case 输出 total/quality/viral/hook 分差
- 2026-04-24：继续收口 `20` 的待回流快速录入闭环：新增 Playwright smoke，确认 warroom 中「快速录入结果」抽屉可直接打开、填写 24h 三个核心数字、保存后关闭，并成功写入 `article_outcome_snapshots`
- 2026-04-24：继续收口 `20` 的 AI 观测看板查询：新增 `ai_call_observation_*_stats` rollup 表，`recordAiCallObservation()` 改为写时增量聚合；看板读取不再对 `ai_call_observations` 做全表聚合，并新增 25k 样本性能回归确认查询耗时低于 1s
- 2026-04-24：继续收口 `20` 的 migration parity：补齐空库 `legacy bootstrap` 与 `migration + compat` 两条初始化路径的 schema snapshot 对比回归，并将 `ai_call_observation_*_stats` 纳入 legacy compat，确认两条路径最终结构一致
- 2026-04-24：继续收口 `20` 的 `schema-bootstrap.ts` 兼容层：新增源码级防回退测试，确认主文件不再内嵌 legacy DDL / 兼容补丁，只保留 migration gate、runtime cache 与 `schema-bootstrap-legacy.ts` 委托；对应工程债从“继续拆分”收敛为“已完成并锁定”
- 2026-04-24：继续收口 `17 / 20` 的数据验收辅助链路：新增 `pnpm plan17:acceptance-blockers` 输出 acceptance 真阻塞明细，新增 `pnpm plan17:quality-label-worklist` / `pnpm plan17:quality-label-import` 形成 `strategy_strength / evidence_hook` 的待标注导出与批量回写闭环；这提升了补样效率，但不改变当前 `partial` 真状态
- 2026-04-24：继续收口 `20` 的 prompt cache 真验收底座：`ai_call_observations` 已补 `article_id` 维度、`generateSceneText()` 已支持 article-linked 观测透传，新增 `pnpm plan20:prompt-cache-report` 与对应回归测试；脚本现也支持 `--write-baseline` / `--baseline-file` 记录并比较同一 `article_id` 的 token 降幅。这让“同一文章二次 deepWrite 的 cache read”与后续 baseline 对账都可被事实化观测，但真实 provider 样本与 baseline 对照仍未完成
- 2026-04-24：继续校正 `21` 的文档状态：`/reviews` 独立「开头体检」入口、工作区显式入口与端到端 smoke 已确认为完成态，剩余表述已收敛为文档归档与业务观察窗
- 2026-04-23：按“业务功能缺口”再次复核 `11-21`：`11` 的决议向导、keep+rollout 一体化事务、继续优化下一轮实验、自动治理与风险台账均已存在；当前剩余未完成项不再属于业务能力空白，而是 `16/20` 体验与工程收口、`17` 数据验收观察窗
- 2026-04-23：继续收口 `20` 的 `schema-bootstrap.ts` 兼容层：将大段 `CREATE TABLE IF NOT EXISTS` 与索引列表外提到 `schema-bootstrap-legacy.ts`，主文件改为通过 helper 装配 legacy SQL，并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`、`pnpm exec tsx --test apps/web/src/lib/__tests__/repositories-prompt-seed.test.ts`、`pnpm exec tsx --test apps/web/src/lib/__tests__/schema-bootstrap-cache.test.ts`
- 2026-04-23：继续收口 `20` 的 `schema-bootstrap.ts` 兼容层：将剩余列级兼容补丁、历史字段 rename/drop 与写作评测/信源兼容修补整体下沉到 `schema-bootstrap-legacy.ts` 的 `applyLegacySchemaCompat()`，主文件进一步退化为 migration + orchestrator，并再次通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false` 与两条 schema 初始化回归测试
- 2026-04-23：继续收口 `20` 的 `schema-bootstrap.ts` 兼容层：将 `ensureTemplateLibrarySeeds()` 里的模板库 seed 与默认 `topic_sources` seed 也下沉到 `schema-bootstrap-legacy.ts`，主文件只保留委托调用；再次通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false` 与两条 schema 初始化回归测试
- 2026-04-23：继续收口 `20` 的 `schema-bootstrap.ts` 兼容层：新增 `schema-bootstrap-db-utils.ts` 承接 `execAll / hasTable / hasColumn / ensureColumn / rename/drop / replaceTextInColumn` 等通用 DB helper，`schema-bootstrap.ts` 进一步只保留 runtime cache 与 orchestration；再次通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false` 与两条 schema 初始化回归测试
- 2026-04-23：继续收口 `20` 的 `schema-bootstrap.ts` 兼容层：将 `topic_sources / asset_files / persona_tags / layout_templates` 回填以及 writing-eval / active-asset seeds 的后置串联整体下沉到 `schema-bootstrap-legacy.ts` 的 `runLegacySchemaPostMigrations()`，主文件进一步退化为 migration gate + cache + 顶层委托；并通过 `pnpm --filter @huoziwriter/web exec tsc --noEmit --pretty false`、`pnpm exec tsx --test apps/web/src/lib/__tests__/repositories-prompt-seed.test.ts apps/web/src/lib/__tests__/schema-bootstrap-cache.test.ts` 与 `pnpm exec tsx --test apps/web/src/lib/__tests__/db-migrations.test.ts`
